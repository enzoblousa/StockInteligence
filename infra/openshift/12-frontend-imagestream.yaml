# Mesma ideia do ImageStream do backend (07-backend-imagestream.yaml):
# a "prateleira" interna onde a imagem do frontend construída pelo
# BuildConfig vai ficar guardada.
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: frontend
  namespace: enzoblousa-dev
